""""""
import numpy as np
from gensim.models.word2vec import Word2Vec


def gen_batch_sg(data, batch_size=8, skip_window=1):
    """

    Args:
        data:
            [[doc1], [doc2], ...]
        batch_size:
        skip_window:

    Returns:

    """

gen_batch_sg()